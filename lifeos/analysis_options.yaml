include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: true
    prefer_single_quotes: true

analyzer:
  errors:
    todo: ignore # TODOs mark the explicit Isar-migration boundary — see README.md
